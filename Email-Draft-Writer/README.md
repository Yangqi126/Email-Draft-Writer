# Email Draft Writer (Pro UI)

This version looks more like a real AI product:
- App layout (sidebar + topbar + two panels)
- Rich but organized visuals (icons, empty-state illustration, quick prompt cards)
- Advanced settings tucked away to reduce clutter

Open `index.html` to preview.
